# Dependencies
node_modules/
# Bun is the canonical toolchain — bun.lock is the single lockfile.
package-lock.json

# Build output
dist/
*.tsbuildinfo

# Local env / secrets (Vercel: `vercel env pull .env.local`)
.env
.env.local
.env*.local

# Vercel
.vercel/

# Local real secrets that Vercel cannot pull (Sensitive vars export empty).
# No ".env" in the name so it survives `vercel env pull` and tooling guards.
.secrets.local

# OS / editor
.DS_Store
.vercel
.env*
.keys/
